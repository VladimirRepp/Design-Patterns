// Задача: создать врагов относительно уровня игрока 

класс Враг 

класс Скилет : Враг 
класс Орк : Враг 
класс Дракон : Враг 

// Проблема: код создания объекта разбросан по пректу 
// Где-то в коде - плохо 
если уровеньИгрока < 5
 создать Скилет
иначе если уровеньИгрока >= 5 и уровеньИгрока < 10
 создать Орк
иначе если уровеньИгрока >= 10
 создать Дракон

// Решение проблемы - фабричный метод
класс ФабрикаВрагов:
 действие СоздатьВрага(уровеньИгрока)
  если уровеньИгрока < 5
   создать Скилет
  иначе если уровеньИгрока >= 5 и уровеньИгрока < 10
   создать Орк
  иначе если уровеньИгрока >= 10
   создать Дракон

// Пример использования
// Не где-то создаются враги, а в конкретном месте 
ФабрикаВрагов враги
Враг враг = враги.СоздатьВрага(5) 

 

