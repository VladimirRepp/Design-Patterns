// Проблема:
// Поведение врага зависит от состояния (Спокойный, Агрессивный, Паника). If-ы расползаются по коду.

// Идея:
// Вынести поведение в отдельные классы состояний. Объект делегирует текущему состоянию.

класс Враг 
 Состояние текущееСостояние 

 дейсвтие Обновить()
  текущееСостояние.Выполнить()

 действие СменитьСостояние(новое)
  текущееСостояние.Выйти()
  текущееСостояние = новое
  текущееСостояние.Войти()

класс Сотяние
 Враг враг 

 действие Войти()
 действие Обновить() 
 действие Выйти()
 
 действие ЗадатьВрага(Враг) // состояние напрямую влияет на объект

класс СостояниеСпокойный : Сотяние 
класс СостояниеАгресивынй : Сотяние 
класс СостояниеПаники : Сотяние 

