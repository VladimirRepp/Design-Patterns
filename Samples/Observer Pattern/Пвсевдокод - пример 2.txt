// Проблема:
// Когда здоровье игрока меняется, нужно обновить: 
// полоску HP, интерфейс, звук биения сердца, логику врагов.

// Идея:
// Объект (Subject) рассылает уведомления подписчикам (Observers).

// Решение: паттерн наблюдатель 

класс Наблюдатель
 действие Обновить(здоровьеИгрока) 

класс ПолоскаХП : Наблюдатель
 переопределить действие Обновить(здоровьеИгрока) // увеличить или уменьшить

класс Интерфейс : Наблюдатель
 переопределить действие Обновить(здоровьеИгрока) // показать сообщение в UI

класс МенеджерЗвуковИгрока : Наблюдатель
 переопределить действие Обновить(здоровьеИгрока) // звук биения сердца

класс AIВрагов : Наблюдатель
 переопределить действие Обновить(здоровьеИгрока) // меняем поведение врага количества жизни игрока

// Сделать подписчиков у игрока и сделать рассылку всем 
класс Игрок 
 список наблюдателей

 действие ДобавитьНаблюдателя(наблюдатель)
 действие УдалитьНаблюдателя(наблюдатель)
 действие УдалитьВсехНаблюдателей()

 действие ОповеститьНаблюдателей()

 действие ИзменитьЖизнь(новоеКоличествоЖизни)
// на событие изменение здоровья у игрока вызываем действие ОповеститьНаблюдателей


